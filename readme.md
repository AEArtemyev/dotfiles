# dotfiles

Личный репозиторий с конфигами для терминальной среды и редактора.

## Установка

```bash
git clone ~/dotfiles
ln -s ~/dotfiles/lazyvim ~/.config/lazyvim
echo '. ~/dotfiles/bash/.bashrc' >> ~/.bashrc
ln -s ~/dotfiles/tmux/.tmux.conf ~/.tmux.conf
```

